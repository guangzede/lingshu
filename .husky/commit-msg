#!/usr/bin/env sh

# 运行 commitlint 检查 commit message
# 优先使用本地安装的可执行文件以避免全局 npx/环境问题
if [ -x "./node_modules/.bin/commitlint" ]; then
	./node_modules/.bin/commitlint --edit "$1"
else
	npx --no -- commitlint --edit "$1"
fi